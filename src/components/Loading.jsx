import React, { useState } from "react";
import GridLoader from "react-spinners/GridLoader";

const Loading = () => {
  return <GridLoader color="#0d6efd" size={30} />;
};

export default Loading;
